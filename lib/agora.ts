// Fixed RTC role UIDs used by the quickstart flow. They are not secrets.
// Override with NEXT_PUBLIC_AGENT_UID / NEXT_PUBLIC_MATCH_FEED_UID only if your
// Agora Media Gateway or backend deployment uses different reserved UIDs.
export const DEFAULT_AGENT_UID = 123456;
export const DEFAULT_MATCH_FEED_UID = 234567;
